pipeline {
    agent any
    stages {
        stage('Build image and deploy to dockerhub') {
            steps {
                step {
                    echo 'Checking docker version'
                }
            }
        }
    }
}
